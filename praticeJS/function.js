// Funtion is to reuse a bunch of the code.
// parameter deosn't have any data type in Javascript

const player = {
    name:"kim",
    sayHello: (name) => {
        console.log("hello!" + name);
    }
};

player.sayHello("Ata");
