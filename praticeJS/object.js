//object => indexing the elements via string  
//array => indexing the elements via number

const playerName = "kim";
const playerPoints = 1212;
const playerHandsome = false;

const player = {
    name:"kim",
    points: 10,
    fat: true,
};

console.log(player);
console.log(player.name);

player.fat = false;