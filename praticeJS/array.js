//To declare an array, [] should be used while for an object, it uses {}.
//for an array can contain different data types.
const mon = "Mon";
const tue = "Tue";
const wed = "Wed";

const daysOfWeek = [mon, tue, wed];
const nonsense = [1, 2, "hello", false, null, undefined];

//index the array
daysOfWeek[5];

//add an element into an array
daysOfWeek.push("sun");
